## 왜 배우나

컴퓨터는 많은 판단을 참과 거짓으로 처리합니다. `if`, `while`, 필터 조건, 권한 검사,
테스트 코드의 기대값은 모두 어떤 문장이 참인지 거짓인지 묻는 구조입니다.

명제 논리는 이런 판단을 수학적으로 다루는 가장 작은 단위입니다. 코드를 더 정확하게
읽고, 조건이 언제 깨지는지 설명하려면 먼저 "판단 가능한 문장"을 작은 단위로 나누는
습관이 필요합니다.

예를 들어 다음 요구사항은 한 문장처럼 보이지만 실제로는 여러 명제가 섞여 있습니다.

- "사용자가 로그인했고, 이메일 인증을 마쳤으며, 정지된 계정이 아니면 글을 쓸 수 있다."
- "상품이 품절이 아니거나 예약 판매 중이면 구매 버튼을 보여 준다."
- "점수가 60점 이상이고 지각이 3회 미만이면 수료 처리한다."

이 문장들을 작은 명제로 나누면 코드 조건식, 테스트 케이스, 예외 상황을 더 차분하게
다룰 수 있습니다.

## 먼저 감 잡기

명제는 참 또는 거짓을 정할 수 있는 문장입니다.

- "7은 홀수이다"는 참인 명제입니다.
- "2 + 2 = 5"는 거짓인 명제입니다.
- "현재 사용자는 로그인했다"는 실행 시점의 상태를 보면 참/거짓을 정할 수 있으므로 명제입니다.
- "이 코드는 좋은가?"는 기준이 없으면 참/거짓을 정하기 어려워 명제로 보기 어렵습니다.
- "버튼을 예쁘게 만들어라"는 명령문이므로 명제가 아닙니다.

프로그래밍에서는 실행 시점의 상태가 정해져 있을 때 참/거짓으로 평가되는 조건식을 명제처럼
다룰 수 있습니다. 다만 수학 명제는 값이 안정적으로 정해진 문장이고, 코드 조건식은 변수
상태나 함수 호출 결과에 따라 실행할 때마다 값이 달라질 수 있습니다.

```ts
const isAdult = age >= 19;
const hasPermission = role === "admin" || role === "editor";
const canAccess = isAdult && hasPermission;
```

여기서 `age >= 19`, `role === "admin"`, `role === "editor"`는 현재 변수 값이 주어지면
참 또는 거짓으로 평가되는 작은 판단입니다. `canAccess`는 이 판단들을 논리 연산으로 조합한
복합 명제처럼 읽을 수 있습니다.

<MultipleChoiceQuiz
  questions={[
    {
      prompt: "`age >= 19 && country === \"KR\"`에 들어 있는 작은 명제의 개수는?",
      choices: ["2개", "1개", "3개", "0개"],
      correctIndex: 0,
      explanation: "`age >= 19`와 `country === \"KR\"` 두 판단을 AND로 묶은 복합 명제입니다.",
    },
  ]}
/>

## 핵심 정의

명제는 참 또는 거짓 중 하나의 진리값을 갖는 문장입니다. 두 명제 P, Q가 있을 때 기본
논리 연산은 다음과 같습니다.

| 연산 | 의미 | 참이 되는 경우 |
| --- | --- | --- |
| $P \land Q$ | AND | P와 Q가 모두 참 |
| $P \lor Q$ | OR | P 또는 Q 중 하나 이상 참 |
| $\lnot P$ | NOT | P가 거짓 |

복합 명제는 작은 명제를 결합해서 만든 명제입니다.

- $P \land Q$: "P이고 Q이다"
- $P \lor Q$: "P이거나 Q이다"
- $\lnot P$: "P가 아니다"
- $(P \land Q) \lor R$: "P와 Q가 모두 참이거나, R이 참이다"

논리식은 한 번에 읽기보다 가장 안쪽 판단부터 읽는 편이 안전합니다. 예를 들어
$(P \land Q) \lor R$은 "P와 Q가 모두 참"이라는 한 덩어리와 "R이 참"이라는 다른 가능성을
OR로 연결한 식입니다. 반면 $P \land (Q \lor R)$은 P가 반드시 참이어야 하고, Q나 R 중
하나가 추가로 참이어야 합니다.

이 차이는 부작용 없는 불리언 조건식이라면 코드에서도 바로 드러납니다.

```ts
const canEnterA = (isMember && hasPass) || isGuest;
const canEnterB = isMember && (hasPass || isGuest);
```

첫 번째 조건은 게스트만 참이어도 입장할 수 있습니다. 두 번째 조건은 게스트 여부와 상관없이
회원이어야 합니다. 괄호는 단순한 표기가 아니라 정책을 바꾸는 장치입니다.

## 자주 헷갈리는 지점

일상어의 "또는"은 가끔 둘 중 하나만 고르는 느낌이지만, 수학의 OR과 많은 프로그래밍
언어의 논리 OR은 보통 둘 다 참인 경우도 참으로 봅니다. "쿠폰이 있거나 포인트가 있으면 할인 가능"이라는
규칙에서 쿠폰과 포인트가 둘 다 있어도 할인 가능인 것과 같습니다.

명제와 의견도 자주 헷갈립니다.

- "이 함수는 10ms 안에 끝난다"는 측정 기준이 있으므로 명제입니다.
- "이 함수는 빠르다"는 빠름의 기준이 없으면 명제로 쓰기 어렵습니다.
- "사용자 이름은 비어 있지 않다"는 명제입니다.
- "사용자 이름을 잘 입력해라"는 명령이므로 명제가 아닙니다.

또 하나의 흔한 실수는 긴 조건식을 한 번에 읽으려는 것입니다. 다음 코드는 한 줄이지만
세 개의 명제가 들어 있습니다.

```ts
const canPublish = isLoggedIn && hasVerifiedEmail && !isSuspended;
```

초보자는 이 코드를 "글을 쓸 수 있는가?"라고만 읽기 쉽습니다. 하지만 디버깅할 때는
`isLoggedIn`, `hasVerifiedEmail`, `isSuspended`를 각각 따로 확인해야 어느 조건 때문에
실패했는지 알 수 있습니다.

<MultipleChoiceQuiz
  questions={[
    {
      prompt: "`!isSuspended`를 명제로 자연스럽게 읽으면?",
      choices: [
        "정지된 계정이 아니다.",
        "정지된 계정이다.",
        "계정이 존재하지 않는다.",
        "정지 여부를 알 수 없다.",
      ],
      correctIndex: 0,
      explanation: "`!`는 NOT입니다. `isSuspended`가 거짓이라는 뜻이므로 정지된 계정이 아니라는 명제입니다.",
    },
  ]}
/>

## 직접 실험하기

<TruthTablePlayground />

### 예제를 해석하는 순서

복합 조건을 만나면 바로 진리표를 외우려고 하지 말고 다음 순서로 읽습니다.

1. 참/거짓을 정할 수 있는 가장 작은 문장을 찾습니다.
2. 각 문장에 P, Q, R 같은 이름을 붙입니다.
3. "그리고", "또는", "아니다"를 각각 $\land$, $\lor$, $\lnot$로 바꿉니다.
4. 코드라면 `&&`, `||`, `!`가 어느 작은 명제에 붙는지 괄호로 확인합니다.

예를 들어 "로그인했고 관리자이거나, 초대 링크가 있다"는 문장은 두 가지로 해석될 수
있습니다.

```ts
(isLoggedIn && isAdmin) || hasInvite
isLoggedIn && (isAdmin || hasInvite)
```

괄호 위치가 달라지면 초대 링크만 있는 사용자의 접근 가능 여부가 달라집니다. 명제 논리는
이런 애매함을 줄이기 위해 작은 판단과 결합 방식을 분리해서 보는 도구입니다.



## 코드로 읽기

논리 연산은 조건문을 구성하는 재료입니다.

```ts
if (isLoggedIn && hasAdminRole) {
  showAdminPanel();
}
```

여기서 `isLoggedIn`과 `hasAdminRole`은 각각 현재 상태에 대한 참/거짓 판단이고, 전체 조건은
두 판단의 AND입니다.

조금 더 복잡한 요구사항도 같은 방식으로 분해할 수 있습니다.

```ts
const hasWritePermission = role === "admin" || role === "editor";
const canWritePost = isLoggedIn && hasWritePermission && !isSuspended;
```

- P: 사용자가 로그인했다.
- Q: 사용자는 관리자이거나 편집자이다.
- R: 사용자는 정지된 계정이다.
- 전체 조건: $P \land Q \land \lnot R$

이렇게 바꾸면 테스트 케이스도 자연스럽게 나옵니다. P, Q, R 중 하나씩 값을 바꿔 보면서
어느 조합에서 글쓰기가 허용되어야 하는지 확인할 수 있습니다.

<MultipleChoiceQuiz
  questions={[
    {
      prompt: 'P = "로그인했다", Q = "쓰기 권한이 있다", R = "정지 계정이다"일 때 P AND Q AND NOT R이 참인 경우는?',
      choices: [
        "로그인했고 쓰기 권한이 있으며 정지 계정이 아니다.",
        "로그인했지만 쓰기 권한이 없다.",
        "쓰기 권한은 있지만 정지 계정이다.",
        "정지 계정이 아니기만 하면 항상 참이다.",
      ],
      correctIndex: 0,
      explanation: "세 조건이 모두 맞아야 합니다. 특히 R은 거짓이어야 `NOT R`이 참입니다.",
    },
  ]}
/>

## 전공에서 만나는 장면

명제 논리는 전공 과목의 큰 제목보다 더 작은 장면에서 반복됩니다.

- 자료구조: 스택이 비어 있는가, 큐가 가득 찼는가, 방문한 정점인가를 판단합니다.
- 데이터베이스: `WHERE active = true AND role = 'admin'`처럼 행을 남길 조건을 만듭니다.
- 테스트: "입력이 빈 문자열이면 에러가 난다"처럼 기대 결과를 참/거짓으로 검증합니다.
- 보안: 로그인 여부, 권한, 토큰 만료 여부를 조합해 접근 가능성을 판단합니다.
- 알고리즘: 반복문이 계속될 조건과 종료될 조건을 명제로 표현합니다.

복잡한 시스템도 결국 작은 판단들의 조합으로 동작합니다. 명제를 잘 나누면 코드의 의도를
설명하기 쉬워지고, 실패한 조건을 찾는 속도도 빨라집니다.

## 종합 점검

<MultipleChoiceQuiz
  questions={[
    {
      prompt: '다음 중 명제로 보기 가장 어려운 것은?',
      choices: [
        "이 함수는 읽기 좋게 작성되었다.",
        "7은 홀수이다.",
        "사용자 이름은 비어 있지 않다.",
        "응답 상태 코드는 200이다.",
      ],
      correctIndex: 0,
      explanation: "읽기 좋다는 기준이 정해져 있지 않으면 참/거짓을 분명히 판단하기 어렵습니다.",
    },
    {
      prompt: 'P가 참이고 Q가 거짓일 때 P AND Q의 값은?',
      choices: ["거짓", "참", "항상 판단 불가", "P와 Q가 같을 때만 참"],
      correctIndex: 0,
      explanation: "AND는 두 명제가 모두 참일 때만 참입니다.",
    },
    {
      prompt: '"쿠폰이 있거나 포인트가 있으면 할인된다"를 가장 가깝게 나타낸 것은?',
      choices: [
        "hasCoupon || hasPoint",
        "hasCoupon && hasPoint",
        "!hasCoupon && hasPoint",
        "!hasCoupon || !hasPoint",
      ],
      correctIndex: 0,
      explanation: "둘 중 하나라도 있으면 되는 조건이므로 OR입니다.",
    },
    {
      prompt: '"로그인했고 이메일 인증을 마쳤다"를 P, Q로 나누면 가장 적절한 것은?',
      choices: [
        'P = "로그인했다", Q = "이메일 인증을 마쳤다"',
        'P = "로그인했다", Q = "로그아웃했다"',
        'P = "이메일이다", Q = "인증이다"',
        'P = "사용자이다", Q = "화면이다"',
      ],
      correctIndex: 0,
      explanation: "각각 참/거짓을 판단할 수 있는 작은 문장으로 나누어야 합니다.",
    },
    {
      prompt: '`isMember && hasPass || isGuest`에서 가장 먼저 조심해야 할 것은?',
      choices: [
        "AND와 OR의 묶임이 의도와 맞는지 확인한다.",
        "변수 이름이 모두 같은 길이인지 확인한다.",
        "OR가 항상 거짓인지 확인한다.",
        "NOT이 없으므로 명제가 아니라고 본다.",
      ],
      correctIndex: 0,
      explanation: "괄호가 없으면 읽는 사람이 정책을 다르게 이해할 수 있습니다.",
    },
    {
      prompt: '수학과 보통 프로그래밍에서 OR에 대한 설명으로 맞는 것은?',
      choices: [
        "둘 다 참이어도 OR는 참이다.",
        "둘 중 정확히 하나만 참이어야 한다.",
        "항상 AND와 같은 뜻이다.",
        "NOT과 함께 쓰일 때만 참이다.",
      ],
      correctIndex: 0,
      explanation: "일반적인 OR는 하나 이상이 참이면 참입니다.",
    },
    {
      prompt: '`const canDelete = isOwner || isAdmin;`을 자연어로 가장 잘 읽은 것은?',
      choices: [
        "소유자이거나 관리자이면 삭제할 수 있다.",
        "소유자이면서 관리자여야 삭제할 수 있다.",
        "관리자가 아니면 항상 삭제할 수 있다.",
        "소유자이면 관리자가 될 수 없다.",
      ],
      correctIndex: 0,
      explanation: "`||`는 OR이므로 둘 중 하나라도 참이면 전체 조건이 참입니다.",
    },
    {
      prompt: '명제 논리가 테스트 설계와 연결되는 방식으로 가장 적절한 것은?',
      choices: [
        "조건을 작은 참/거짓 판단으로 나누어 케이스를 만든다.",
        "모든 테스트 이름을 수식으로 바꾼다.",
        "테스트에서는 OR를 사용하지 않는다.",
        "참인 경우만 테스트하고 거짓인 경우는 제외한다.",
      ],
      correctIndex: 0,
      explanation: "조건을 작은 참/거짓 판단으로 나누면 어떤 조합을 테스트해야 하는지 보입니다.",
    },
  ]}
/>

## 다음으로

다음 챕터에서는 조건문 $P \to Q$와 진리표를 더 자세히 봅니다. 특히 조건문이 왜
한 가지 경우에만 거짓이 되는지 코드의 사전조건과 연결합니다.
