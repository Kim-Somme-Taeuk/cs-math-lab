## 왜 배우나

컴퓨터공학에서는 값을 하나씩만 다루지 않습니다. 사용자 목록, 검색 결과, 태그 집합,
그래프의 정점 집합처럼 여러 대상을 묶어서 다루는 일이 많습니다.

집합은 "무엇이 포함되는가"를 기준으로 데이터를 생각하게 해 줍니다. 이 관점은 중복 제거,
권한 비교, 검색 필터, 방문 여부 기록처럼 실무 코드에서도 자주 등장합니다.

## 먼저 감 잡기

집합은 서로 구분되는 원소들의 모음입니다. 순서는 중요하지 않고, 같은 원소를 여러 번
써도 한 번만 포함된 것으로 봅니다.

예를 들어 학습한 챕터 이름을 모은다고 해 봅시다.

```txt
원래 목록: logic, sets, logic
집합으로 본 결과: logic, sets
```

Python의 `set`은 이 느낌을 바로 보여 줍니다.

```python
chapters = {"logic", "sets", "logic"}
print(chapters)
```

`logic`을 두 번 써도 집합의 원소로는 한 번만 남습니다. Python과 JavaScript 모두 집합과
비슷한 자료구조를 제공하지만, 이 장에서는 수학 개념과 가장 직접적으로 연결되는 Python
`set` 연산을 먼저 사용합니다.

일상 예시로 보면 더 쉽습니다.

- 수강생 집합: 어떤 학생이 수업에 포함되는지 묻습니다.
- 장바구니 상품 집합: 어떤 상품 종류가 담겼는지 묻습니다.
- 검색 결과 집합: 어떤 문서가 조건을 만족하는지 묻습니다.
- 방문한 페이지 집합: 어떤 URL을 이미 처리했는지 묻습니다.

<MultipleChoiceQuiz
  questions={[
    {
      prompt: "`{\"logic\", \"sets\", \"logic\"}`의 원소 개수는?",
      choices: ["2개", "3개", "1개", "판단할 수 없음"],
      correctIndex: 0,
      explanation: "집합은 중복을 한 번만 세므로 `logic`과 `sets` 두 원소만 남습니다.",
    },
  ]}
/>

## 핵심 정의

집합 A와 B가 있을 때 자주 쓰는 연산은 다음과 같습니다.

| 연산 | 표기 | 의미 |
| --- | --- | --- |
| 합집합 | $A \cup B$ | A 또는 B에 속한 원소 |
| 교집합 | $A \cap B$ | A와 B에 모두 속한 원소 |
| 차집합 | $A - B$ | A에는 있고 B에는 없는 원소 |
| 여집합 | $A^c$ | 전체집합 U에는 있지만 A에는 없는 원소 |

부분집합도 중요합니다. $A \subseteq B$는 A의 모든 원소가 B에도 들어 있다는 뜻입니다.
예를 들어 "사용자가 가진 권한이 필요한 권한을 모두 포함하는가?"는 부분집합 검사로 볼 수
있습니다.

부분집합은 원소 개수만으로 판단할 수 없습니다. A의 원소가 B에 모두 들어 있는지가 핵심입니다.
예를 들어 $A = \{1, 2\}$, $B = \{2, 3, 4\}$라면 A의 원소 개수가 더 적어도 A는 B의
부분집합이 아닙니다. 1이 B에 없기 때문입니다.

## 자주 헷갈리는 지점

집합에서는 순서와 중복을 무시합니다. `[1, 2]`와 `[2, 1]`은 배열로는 다르지만, 집합으로는
같은 원소를 가진 같은 집합입니다. `[1, 1, 2]`도 집합으로 보면 `{1, 2}`입니다.

여집합은 반드시 전체집합 U가 있어야 의미가 정해집니다. "A가 아닌 것"이 어디까지인지
정해야 하기 때문입니다. 학생 명단 안에서 A가 아닌 것인지, 전체 사람 중 A가 아닌 것인지에
따라 결과가 달라집니다.

합집합과 교집합도 코드에서 헷갈리기 쉽습니다.

- "둘 중 하나라도 만족"이면 합집합입니다.
- "둘 다 만족"이면 교집합입니다.
- "A 조건은 만족하지만 B 조건은 제외"이면 차집합입니다.

검색 필터에서 "프론트엔드 또는 백엔드 태그"는 합집합이고, "프론트엔드이면서 React 태그"는
교집합입니다.

전체집합을 정하지 않은 여집합도 위험합니다. "프론트엔드가 아닌 항목"은 전체가 전체 문서인지,
현재 검색 결과인지, 현재 사용자가 볼 수 있는 문서인지에 따라 달라집니다. 그래서 코드에서는
여집합을 만들기 전에 기준이 되는 전체 목록을 먼저 정해야 합니다.

<MultipleChoiceQuiz
  questions={[
    {
      prompt: "검색 결과 X와 즐겨찾기 Y의 교집합은 무엇을 뜻하는가?",
      choices: [
        "검색 결과이면서 즐겨찾기에도 들어 있는 항목",
        "검색 결과와 즐겨찾기를 모두 합친 항목",
        "검색 결과에는 있지만 즐겨찾기에는 없는 항목",
        "검색 조건을 만족하지 않는 모든 항목",
      ],
      correctIndex: 0,
      explanation: "교집합은 두 집합에 동시에 들어 있는 원소만 남깁니다.",
    },
  ]}
/>

## 직접 실험하기

<SetVennPlayground />

### 예제를 해석하는 순서

집합 문제는 "원소 하나를 집어넣어 보면 어디에 남는가?"로 판단하면 쉽습니다.

1. 전체집합 U를 먼저 확인합니다.
2. 원소 x가 A에 들어 있는지 묻습니다.
3. 원소 x가 B에 들어 있는지 묻습니다.
4. 연산 기호가 "둘 중 하나", "둘 다", "A만" 중 무엇을 요구하는지 확인합니다.

예를 들어 검색 결과 A와 북마크 B가 있을 때 `A - B`는 "검색은 되었지만 아직 북마크하지
않은 항목"입니다. 수식만 보면 추상적이지만, 원소 하나를 기준으로 질문하면 코드의
필터 조건과 바로 연결됩니다.

## 코드로 읽기

집합 연산은 먼저 자연어로 나누면 코드도 덜 부담스럽습니다.

- A: 검색 결과에 들어 있는 항목
- B: 북마크에 들어 있는 항목
- $A \cup B$: 검색 결과이거나 북마크인 항목
- $A \cap B$: 검색 결과이면서 북마크인 항목
- $A - B$: 검색 결과에는 있지만 북마크에는 없는 항목

Python에서는 수학 기호와 비슷한 연산자를 사용할 수 있습니다.

```python
a = {1, 2, 3}
b = {3, 4, 5}

union = a | b
intersection = a & b
difference = a - b
```

`|`는 합집합 $\cup$, `&`는 교집합 $\cap$, `-`는 차집합에 대응합니다. 교집합은 "A에도 있고
B에도 있는 원소만 남긴다"이고, 차집합은 "A에는 있지만 B에는 없는 원소만 남긴다"입니다.

권한 검사도 집합으로 읽을 수 있습니다.

```txt
required: 꼭 필요한 권한 집합
granted: 사용자가 가진 권한 집합
질문: required의 모든 원소가 granted에도 있는가?
```

```python
required = {"read", "write"}
granted = {"read", "write", "delete"}

can_proceed = required <= granted
```

`<=`는 Python에서 부분집합 여부를 묻는 연산입니다. 즉 `required`가 `granted`의 부분집합이면
필요한 권한을 모두 가진 것입니다.

<MultipleChoiceQuiz
  questions={[
    {
      prompt: "`required`가 `granted`의 부분집합인지 확인하는 코드에 가장 가까운 것은?",
      choices: [
        "`required <= granted`",
        "`required | granted`",
        "`required - granted`",
        "`granted.clear()`",
      ],
      correctIndex: 0,
      explanation: "필요한 권한이 모두 있는지 봐야 하므로 부분집합 검사 `<=`가 맞습니다.",
    },
  ]}
/>

## 전공에서 만나는 장면

집합은 다음처럼 작은 사례로 연결됩니다.

- 데이터베이스: `UNION`, `INTERSECT`, `EXCEPT`는 합집합, 교집합, 차집합에 가깝습니다. 다만
  SQL은 중복 행 처리, `NULL`, 컬럼 구조 같은 규칙이 있어 수학의 집합 연산과 완전히 같지는
  않습니다.
- 검색: 여러 필터 조건의 결과를 합치거나 겹치는 항목만 남깁니다.
- 그래프 탐색: 이미 방문한 정점을 `visited` 집합에 넣어 중복 방문을 막습니다.
- 권한 시스템: 필요한 권한 집합이 사용자의 권한 집합에 포함되는지 검사합니다.
- 캐시: 이미 계산한 키의 집합을 두고 같은 작업을 반복하지 않습니다.

큰 범주로 "자료구조에 쓰인다"라고 외우기보다, 포함 여부를 빠르게 묻는 장면을 떠올리면
집합이 왜 필요한지 더 잘 보입니다.

## 종합 점검

전체집합 $U = \{1,2,3,4,5,6\}$, $A = \{1,2,3\}$, $B = \{3,4,5\}$라고 하자.

<MultipleChoiceQuiz
  questions={[
    {
      prompt: 'A ∪ B는 무엇인가?',
      choices: ["{ 1, 2, 3, 4, 5 }", "{ 3 }", "{ 1, 2 }", "{ 6 }"],
      correctIndex: 0,
      explanation: "합집합은 A 또는 B에 들어 있는 원소를 모두 모읍니다.",
    },
    {
      prompt: 'A ∩ B는 무엇인가?',
      choices: ["{ 3 }", "{ 1, 2, 3, 4, 5 }", "{ 1, 2 }", "{ 4, 5 }"],
      correctIndex: 0,
      explanation: "교집합은 A와 B에 동시에 들어 있는 원소만 남기므로 3입니다.",
    },
    {
      prompt: 'A - B는 무엇인가?',
      choices: ["{ 1, 2 }", "{ 4, 5 }", "{ 3 }", "{ 1, 2, 3, 4, 5 }"],
      correctIndex: 0,
      explanation: "A - B는 A에는 있지만 B에는 없는 원소입니다.",
    },
    {
      prompt: 'A의 여집합은 무엇인가?',
      choices: ["{ 4, 5, 6 }", "{ 1, 2, 3 }", "{ 3 }", "{ 1, 2, 3, 4, 5 }"],
      correctIndex: 0,
      explanation: "여집합은 전체집합 U에서 A에 들어 있지 않은 원소입니다.",
    },
    {
      prompt: 'A가 B의 부분집합인지 판단하면?',
      choices: [
        "아니다. 1과 2가 B에 없기 때문이다.",
        "맞다. 3을 공유하기 때문이다.",
        "맞다. A의 원소가 더 적기 때문이다.",
        "판단하려면 순서가 필요하다.",
      ],
      correctIndex: 0,
      explanation: "부분집합이 되려면 A의 모든 원소가 B에 있어야 합니다.",
    },
    {
      prompt: '`[1, 2]`와 `[2, 1]`을 집합으로 볼 때 올바른 설명은?',
      choices: [
        "같은 원소를 가지므로 같은 집합으로 볼 수 있다.",
        "순서가 다르므로 반드시 다른 집합이다.",
        "배열은 절대 집합으로 해석할 수 없다.",
        "중복이 없으면 집합이 아니다.",
      ],
      correctIndex: 0,
      explanation: "집합은 순서를 보지 않고 어떤 원소가 들어 있는지를 봅니다.",
    },
    {
      prompt: '`a | b`는 어떤 연산과 가장 가까운가?',
      choices: ["합집합", "교집합", "차집합", "여집합"],
      correctIndex: 0,
      explanation: "A와 B의 원소를 모두 모으므로 합집합입니다.",
    },
    {
      prompt: '`a & b`는 어떤 연산과 가장 가까운가?',
      choices: ["교집합", "합집합", "A의 여집합", "B - A"],
      correctIndex: 0,
      explanation: "A에도 있고 B에도 있는 원소만 남기므로 교집합입니다.",
    },
    {
      prompt: '그래프 탐색에서 `visited` 집합을 쓰는 이유는?',
      choices: [
        "이미 방문한 정점을 다시 처리하지 않기 위해서",
        "정점 이름을 숫자로 바꾸기 위해서",
        "간선의 방향을 모두 없애기 위해서",
        "모든 정점을 항상 삭제하기 위해서",
      ],
      correctIndex: 0,
      explanation: "방문 집합은 중복 방문과 무한 반복을 막는 데 쓰입니다.",
    },
  ]}
/>

## 다음으로

다음 챕터에서는 집합의 원소를 다른 집합의 원소와 대응시키는 함수 개념으로 넘어갑니다.
