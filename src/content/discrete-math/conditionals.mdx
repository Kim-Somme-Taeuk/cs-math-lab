## 1. 이 개념이 필요한 이유

조건문은 컴퓨터공학에서 매우 자주 등장합니다. 함수의 입력 조건, 테스트의 기대 결과,
알고리즘 증명의 "만약 ...라면 ..." 구조가 모두 조건문입니다.

수학의 조건문 $P \to Q$를 이해하면 코드에서 어떤 조건이 깨졌는지 더 정확하게 설명할 수
있습니다.

## 2. 직관적인 설명

$P \to Q$는 "P라면 Q이다"라는 약속입니다.

예를 들어 P가 "비가 온다", Q가 "우산을 가져간다"라면 조건문은 "비가 오면 우산을
가져간다"입니다. 이 약속이 실제로 깨지는 경우는 비가 왔는데 우산을 가져가지 않은
경우뿐입니다.

## 3. 정확한 정의

조건문 $P \to Q$는 P가 참이고 Q가 거짓일 때만 거짓입니다. 나머지 경우는 모두 참입니다.

| P | Q | $P \to Q$ |
| --- | --- | --- |
| 참 | 참 | 참 |
| 참 | 거짓 | 거짓 |
| 거짓 | 참 | 참 |
| 거짓 | 거짓 | 참 |

논리식으로는 다음과 같이 바꿔 쓸 수 있습니다.

$$
P \to Q \equiv \lnot P \lor Q
$$

## 4. 헷갈리는 부분

P가 거짓인데 조건문이 참인 경우가 가장 낯설게 느껴집니다. 하지만 조건문은 "P인 상황에서
Q가 따라오는가"를 검사합니다. P가 일어나지 않았다면 아직 약속 위반 사례가 없습니다.

프로그래밍의 사전조건도 비슷합니다. 특정 입력 범위에서만 결과를 보장하는 함수는 그
입력 범위 밖에 대해서는 다른 규칙을 적용할 수 있습니다.

## 5. 인터랙티브 예시

<ConditionalPlayground />

## 6. 손풀이 문제

<MultipleChoiceQuiz
  questions={[
    {
      prompt: 'P = "배열이 정렬되어 있다", Q = "이진 탐색을 적용할 수 있다"일 때 P -> Q가 거짓인 경우는?',
      choices: [
        "배열이 정렬되어 있는데 이진 탐색을 적용할 수 없다.",
        "배열이 정렬되어 있고 이진 탐색을 적용할 수 있다.",
        "배열이 정렬되어 있지 않은데 이진 탐색을 적용할 수 있다.",
        "배열이 정렬되어 있지 않고 이진 탐색을 적용할 수 없다.",
      ],
      correctIndex: 0,
      explanation: "조건문은 P가 참이고 Q가 거짓일 때만 거짓입니다.",
    },
    {
      prompt: "P가 거짓이고 Q도 거짓일 때 P -> Q의 값은?",
      choices: ["참", "거짓", "항상 오류", "Q만 보고 결정할 수 없음"],
      correctIndex: 0,
      explanation: "P가 거짓이면 약속을 검사할 상황이 아니므로 수학적 조건문에서는 참으로 봅니다.",
    },
    {
      prompt: '"n이 짝수라면 n % 2는 0이다"에서 P와 Q로 가장 적절한 것은?',
      choices: [
        'P = "n이 짝수이다", Q = "n % 2는 0이다"',
        'P = "n이다", Q = "짝수이다"',
        'P = "n % 2이다", Q = "0이다"',
        'P = "n은 홀수이다", Q = "n % 2는 1이다"',
      ],
      correctIndex: 0,
      explanation: "조건문 P -> Q는 앞의 조건을 P, 따라오는 결론을 Q로 둡니다.",
    },
  ]}
/>

## 7. 코드 연결

```ts
function binarySearch(sortedNumbers: number[], target: number) {
  // 전제: sortedNumbers는 오름차순으로 정렬되어 있다.
  // 이 전제가 참이면, 탐색 결과의 정확성을 기대할 수 있다.
}
```

알고리즘 설명에서 전제가 P이고 결론이 Q라면, 정확성 주장은 보통 $P \to Q$ 형태를 갖습니다.

## 8. 전공 연결

조건문은 알고리즘 증명, 타입 가드, 테스트 조건, 보안 정책에서 반복됩니다. "어떤 입력이면
어떤 결과가 보장된다"는 문장을 다루는 능력은 전공 과목 전반에 필요합니다.

## 9. 다음 개념

다음 챕터에서는 여러 값을 묶어서 다루는 집합을 배웁니다. 조건문이 판단의 기본 단위라면,
집합은 데이터를 묶고 비교하는 기본 단위입니다.
